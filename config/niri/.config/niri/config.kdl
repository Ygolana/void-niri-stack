// Ensure Qt applications (like Quickshell) use the Wayland backend
environment "QT_QPA_PLATFORM" "wayland"

// Spawn Noctalia when Niri starts
spawn-at-startup "/usr/local/bin/qs" "-c" "${HOME}/.local/share/noctalia" "--no-duplicate"

// … (the rest of your configuration: binds, layout, etc.)
