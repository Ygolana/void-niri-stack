// Existing “binds” and layout sections remain unchanged ...
// Just fix the spawn line (should be the only one under “spawn-at-startup”):
spawn-at-startup "qs" "-c" "${HOME}/.local/share/noctalia" "--no-duplicate"
